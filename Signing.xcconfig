// Signing identity. "-" is ad-hoc, which works on any machine.
// scripts/make-dev-cert.sh writes Signing.local.xcconfig (git-ignored) to switch this machine to a stable
// self-signed certificate, so macOS keeps the Accessibility and Microphone grants across rebuilds.
CODE_SIGN_IDENTITY = -
#include? "Signing.local.xcconfig"
